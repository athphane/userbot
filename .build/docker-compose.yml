version: '3.5'

services:
  userbot:
    build:
      context: ../
      dockerfile: .build/Dockerfile
      x-bake:
        platforms:
          - linux/amd64
          - linux/arm64
        pull: true
        tags:
          - ghcr.io/athphane/userbot
    hostname: userbot
